# 修復 Excel 匯出功能計畫

## Goal
使用已安裝的 `xlsx` (SheetJS) 庫來替換現有的 CSV 匯出邏輯，確保生成的檔案是真正的 Excel (.xlsx) 格式，解決格式與編碼相容性問題。

## Proposed Changes

### [WhiteboardClient.tsx](file:///Users/user/Desktop/電子白板/whiteboard-nextjs/src/app/WhiteboardClient.tsx)

#### [MODIFY] WhiteboardClient.tsx
1.  **引入 `xlsx` 庫**
    *   `import * as XLSX from 'xlsx'`
2.  **重寫 `exportToExcel`**
    *   移除舊的 CSV 拼接邏輯。
    *   使用 `XLSX.utils.json_to_sheet` 將資料轉換為工作表。
    *   使用 `XLSX.utils.book_new` 和 `XLSX.utils.book_append_sheet` 建立工作簿。
    *   使用 `XLSX.writeFile` 下載 `.xlsx` 檔案。

## Verification Plan

### Manual Verification
1.  **匯出測試**
    *   點擊廠商、工務、待處理表格的匯出按鈕。
    *   確認下載的檔案副檔名為 `.xlsx`。
    *   確認 Excel 能開啟且中文顯示正常，欄位正確。
