# 資安缺口修補任務

## 步驟一：新建安全元件 ✅
- [x] `src/lib/crypto.ts` — SHA-256 + Key-stretching（前端）
- [x] `src/lib/crypto-server.ts` — SHA-256 + Key-stretching（後端 Node.js）
- [x] `src/lib/recaptcha.ts` — reCAPTCHA v3 前端工具
- [x] `src/app/api/verify-recaptcha/route.ts` — reCAPTCHA 後端驗證 API
- [x] `src/lib/error-logger.ts` — ScriptErrors Server Action
- [x] `src/components/ErrorBoundary.tsx` — React ErrorBoundary
- [x] `src/components/GlobalErrorHandler.tsx` — 全域錯誤攔截
- [x] `sql/fix_anon_select.sql` — RLS 匿名讀取修正

## 步驟二：修改現有元件 ✅
- [x] `src/lib/validations/schemas.ts` — 密碼強度 count >= 4 改為 >= 3
- [x] `src/lib/supabase/client.ts` — sessionStorage adapter
- [x] `src/app/layout.tsx` — ErrorBoundary + GlobalErrorHandler 包裹

## 步驟三：表單元件整合 ✅
- [x] `LoginClient.tsx` — SHA-256 + reCAPTCHA + Fallback 遷移
- [x] `RegisterClient.tsx` — SHA-256 + reCAPTCHA
- [x] `ForgotPasswordClient.tsx` — reCAPTCHA
- [x] `ResetPasswordClient.tsx` — SHA-256

## 步驟四：後端路由 ✅
- [x] `api/admin/users/route.ts` — server-side SHA-256 hash

## 步驟五：驗證 ✅
- [x] 瀏覽器驗證 — 登入頁兩正常渲染、無 JS 錯誤
- [ ] 使用者需執行 `sql/fix_anon_select.sql` 於 Supabase Dashboard
- [ ] 使用者需設定 `.env.local` 中 reCAPTCHA 金鑰
