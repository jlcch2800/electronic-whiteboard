# 施工回報與施工文件異動紀錄

## 完成項目

### 1. 施工回報/施工文件異動紀錄
- 建立共用日誌模組 `src/lib/change-log.ts`
- 新增、修改、刪除操作皆由資料庫 trigger 自動記錄
- 移除頁面上重複的 `logSystemChange()` 呼叫 (避免重複紀錄)

### 2. 施工文件編輯頁面改版
- 將 URL 輸入欄位改為檔案上傳按鈕 (與新增頁面一致)
- 顯示現有檔案預覽，選擇新檔案後會替換
- 支援 Cloudinary 上傳

---

## 修改檔案清單

| 檔案 | 變更內容 |
|------|----------|
| `src/lib/change-log.ts` | 新增共用日誌模組 |
| `src/app/work-report/new/page.tsx` | 移除 logSystemChange |
| `src/app/work-report/[id]/edit/page.tsx` | 移除 logSystemChange |
| `src/app/work-report/WorkReportClient.tsx` | 移除 logSystemChange |
| `src/app/work-file/new/page.tsx` | 移除 logSystemChange |
| `src/app/work-file/[id]/edit/page.tsx` | 改為檔案上傳按鈕、移除 logSystemChange |
| `src/app/work-file/WorkFileClient.tsx` | 移除 logSystemChange |
